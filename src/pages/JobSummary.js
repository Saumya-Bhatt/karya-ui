import React from "react";
import { Typography } from "@mui/joy";

function JobSummary() {
    return (
        <div>
            <Typography level="h3">Job Summary</Typography>
            <p>This is the page to get job summaries.</p>
        </div>
    );
}

export default JobSummary;
