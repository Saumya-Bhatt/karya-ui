import React from "react";
import { Typography, Box } from "@mui/joy";

function SlackMessageAction() {
    return (
        <Box>
            <Typography level="body1">
                Send a message to slack
            </Typography>
        </Box>
    );
}

export default SlackMessageAction;
